/**
 * Placeholder entry point for the TypeScript migration (Phase 1).
 * Runtime code is still served from lib/ and bin/; this file exists so the
 * build pipeline has a valid input and will be replaced as modules are
 * migrated under src/ in later phases.
 */

export {};
